# 当たり判定シミュレーター 仕様書

## 目的
ゲームの「当たり判定」と「衝突応答（重なったら押し戻す処理）」を、スマホで触りながら体感で学ぶための実験用Webページ。
ゲーム性は不要。シンプルさ最優先。

## 技術スタック
- Vite + TypeScript
- 描画：Three.js
- 物理：Rapier（`@dimforge/rapier3d-compat`）
- 公開：GitHub Pages（GitHubの無料公開機能）
- 新規GitHubリポジトリとして **public** で作成
- APIキーやパスワードなどの秘密情報は一切コードに含めないこと

## 画面・空間
- 平らな床と、外周を囲む低い壁だけのシンプルな空間（床は約30×30）
- 背景・床は落ち着いた単色。オブジェクトは種類ごとに色分け
- スマホ縦持ち・横持ちどちらでも崩れないこと

## プレイヤー
- 箱（四角）1個。色は目立つ色で固定
- 物理エンジン上の動く物体（dynamic body）として扱い、ぶつかったら押し返される・押し返すを体感できるようにする
- 回転はロック（転がらないように）

## 操作（スマホ・タッチ）
- 画面左下に仮想スティック。ドラッグ方向に移動、離すと止まる
- PCでも確認できるよう、WASD/矢印キーでも動くようにしておく

## カメラ
- 斜め上から見下ろす視点で、プレイヤーをなめらかに追従

## 敵オブジェクト（見た目と判定が一致するもの）
| 種類 | 判定の形 | 動き |
|---|---|---|
| 球 | ball | ランダム |
| 箱 | cuboid | ランダム |
| カプセル | capsule | ランダム |
| 円柱 | cylinder | ランダム |
| 円錐 | cone | ランダム |
| 凸包（岩・ダイヤ風） | convexHull | ランダム |
| 複合形（雪だるま＝球2個、ダンベル＝球2個＋円柱） | 複数colliderを1つのbodyに付ける | ランダム |
| ドーナツ（置物） | trimesh（形そのもの） | 固定。穴をプレイヤーが通り抜けられることを確認用 |
| 回るハンマー | cuboid | 位置固定で一定速度で回転し続ける（kinematic body） |

- 「ランダムに動く」は、数秒ごとにランダムな方向へ軽く力を加える（impulse）方式でよい
- 回るハンマーは2〜3本配置

## ズレ体験オブジェクト（見た目と判定をわざと変える）
| 見た目 | 判定 | 体験できること |
|---|---|---|
| ドーナツ | 球 | 穴があるのに通れず弾かれる |
| 星型（平たい板） | 箱 | 何もない角の部分に当たる |
| 細い棒 | 大きな球 | 離れているのに当たる |

- 通常のドーナツ（trimesh）と並べて置き、違いを比べられるようにする

## UI（画面上部に小さく）
- **判定表示ボタン**：ON/OFF。ONでRapierの`debugRender()`を使い、全オブジェクトの判定の形を線で表示
- **数スライダー**：敵オブジェクトの数（各種類の個数）を 1〜5 で変更。変えたら配置し直す
- **リセットボタン**：全部を初期配置に戻す

## 当たったことの見せ方
- プレイヤーと接触した瞬間、その敵オブジェクトを0.2秒ほど白く光らせる（Rapierの衝突イベントを使う）
- プレイヤー自身も一瞬光る

## 非目標（やらないこと）
- スコア、ゲームオーバー、音、タイトル画面
- 凝ったモデルやテクスチャ

## 余裕があれば（後回しでOK）
- 「検知のみモード」切替：判定をsensor（すり抜けるが重なりは検知する設定）に変えて、応答あり／なしを比べる
- 高速で飛ぶ弾を撃てるボタン＋CCD（速すぎてすり抜けるのを防ぐ設定）のON/OFF切替で、すり抜けバグを体感

## 完了条件
- スマホ実機で、仮想スティックでプレイヤーを動かし、全種類の敵とぶつかって押し返されることを確認できる
- 判定表示ONで、ズレ体験オブジェクトの判定と見た目の違いが一目で分かる
- 数スライダーで最大数にしてもスマホでカクつかない
- GitHub Pagesの公開URL（https://ユーザー名.github.io/リポジトリ名/）から動く

## 進め方
1. 床・壁・プレイヤー・仮想スティック・カメラ追従だけ作って動作確認
2. 基本形5種を追加
3. 凸包・複合形・ドーナツ・回るハンマーを追加
4. ズレ体験オブジェクトを追加
5. UI（判定表示・スライダー・リセット）と接触時の光る演出
6. GitHub Pagesにデプロイ
   - `vite.config.ts` に `base: '/リポジトリ名/'` を設定
   - GitHub Actions（pushしたら自動で動く仕組み）で、mainへのpush時に自動ビルド＆Pagesへ公開するワークフローを `.github/workflows/deploy.yml` に作成
   - README に公開URLを記載
各ステップ終わりでコミットすること。
